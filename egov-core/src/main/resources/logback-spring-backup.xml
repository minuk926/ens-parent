<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">
	<include resource="org/springframework/boot/logging/logback/defaults.xml"/>
	<include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

	<appender name="STDOUT"
		class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
<!--			<pattern>[logback]%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>-->
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %clr(${LOG_LEVEL_PATTERN:-%5p}){green} %clr([%18thread]){magenta} %clr(%-40.40logger{39}%line){cyan} %clr(: %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}){faint}</pattern>
		</encoder>
	</appender>

	<logger name="java.sql" level="DEBUG" />
	<logger name="egovframework" level="DEBUG" />
	<logger name="org.egovframe" level="DEBUG" />
	<logger name="jdbc.sqltiming" level="DEBUG" />
	<logger name="org.springframework" level="DEBUG" />

	<!-- ************************************************************************* -->
	<!-- SQL log -->
	<!-- ************************************************************************* -->
	<logger name="org.mybatis.spring.SqlSessionUtils" level="OFF" additivity="false"/>
	<logger name="jdbc" level="OFF" additivity="false"/>
	<!-- SQL문과 소요된 시간 -->
	<logger name="jdbc.sqltiming" level="DEBUG" additivity="false">
		<appender-ref ref="CONSOLE"/>
		<!--appender-ref ref="ASYNC_ROLLING"/-->
	</logger>

	<!-- SQL 결과 조회된 데이터를 table 형식으로 출력 -->
	<logger name="jdbc.resultsettable" level="OFF" additivity="false"/>
	<logger name="jdbc.sqlonly" level="OFF"/>
	<!-- result를 포함한 모든 JDBC 호출 정보 -->
	<logger name="jdbc.resultset" level="OFF" additivity="false"/>
	<!-- DB connection open/close -->
	<logger name="jdbc.connection" level="DEBUG" additivity="false"/>
	<!-- resultset을 제외한 JDBC 호출 정보 -->
	<logger name="jdbc.audit" level="OFF" additivity="false"/>

	<logger name="com.zaxxer.hikari.pool.HikariPool" level="ERROR" additivity="false"/>
	<!-- ************************************************************************* -->
	<!-- SQL log -->
	<!-- ************************************************************************* -->

	<root level="DEBUG">
		<appender-ref ref="STDOUT" />
	</root>

</configuration>
